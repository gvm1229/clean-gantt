#!/bin/sh
# Conventional commit ?? ?? (feat:, fix:, docs: ?)
if ! pnpm exec commitlint --edit "$1" 2>/dev/null; then
    echo "?? ???? ??? ?? ????. type(?: feat, fix, delete)? ??? ??? ???."
    exit 1
fi
