#!/bin/sh
# Husky pre-commit: ????? ??? Prettier ?? ??
# --allow-empty: ??? ????? ??(???? ???? ?? ??)?? ? ??
pnpm exec lint-staged --allow-empty

# OMC rule snapshot ??? ? drift ? ??? ?? stage
node scripts/sync-omc-directives.mjs
git add .agents/directives/omc 2>/dev/null || true

# ??? ??? ?? ?? ?? (?? ?? ? ?? ??)
pnpm vitest run
